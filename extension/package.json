{
  "name": "rebrow-extension",
  "version": "0.1.0",
  "description": "Rebrow - Remote Browser Control Extension",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsc --project . && vite build --config vite.config.mts",
    "watch": "vite build --watch --config vite.config.mts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.315",
    "typescript": "^5.8.2",
    "vite": "^5.4.21",
    "vite-plugin-static-copy": "^3.1.1"
  },
  "dependencies": {
    "zustand": "^5.0.8"
  }
}
